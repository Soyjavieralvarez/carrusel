const carouselV1 = (carouselType) => {
    const slides$$ = document.querySelectorAll(`.slider__slide--${carouselType}`);
    const image$$ = document.querySelectorAll(`.carouselV1__asset--${carouselType}`)
}